# Frontend build/runtime flags (Vite). Never put secrets here.
VITE_API_BASE=/api/v1
# project_deploy button is hidden/disabled unless this is exactly "true"
VITE_PROJECT_DEPLOY_ENABLED=false
# Local vite dev proxy target (platform listens on 8081)
VITE_PROXY_TARGET=http://127.0.0.1:8081
