[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["platform", "agent"]
addopts = "-q"

[tool.ruff]
line-length = 100
target-version = "py311"
src = ["platform", "agent", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
ignore = ["B008"]
